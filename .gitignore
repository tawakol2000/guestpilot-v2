node_modules/
.next/
dist/
# Only ignore top-level build/ (bundler output). Feature modules named
# "build" (e.g. frontend/components/build/) must be tracked — sprint 045
# introduced the /build tuning-surface directory.
/build/
backend/build/
.env
.env.*
!.env.example
*.log
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~
pnpm-lock.yaml
package-lock.json
.pnpm-debug.log*

# 2026-05-15: Claude Code agent worktrees — scratch directories created
# by parallel agent runs. Each holds an ephemeral clone of the repo for
# isolated agent work and should never be committed.
.claude/worktrees/
